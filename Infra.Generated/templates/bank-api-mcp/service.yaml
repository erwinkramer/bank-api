---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "bank-api-mcp-service"
  labels:
    app.kubernetes.io/name: "{{ .Chart.Name }}"
    app.kubernetes.io/component: "bank-api-mcp"
    app.kubernetes.io/instance: "{{ .Release.Name }}"
spec:
  type: "ClusterIP"
  selector:
    app.kubernetes.io/name: "{{ .Chart.Name }}"
    app.kubernetes.io/component: "bank-api-mcp"
    app.kubernetes.io/instance: "{{ .Release.Name }}"
  ports:
    - name: "http"
      protocol: "TCP"
      port: {{ .Values.parameters.bank_api_mcp.port_http | int }}
      targetPort: {{ .Values.parameters.bank_api_mcp.port_http | int }}
