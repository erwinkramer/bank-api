extends:
  - ruleset.bank.yml # Bank API project ruleset
  - ruleset.nlgov.yml # Dutch Public Sector (NLGov) REST API Design Rules, includes Spectral "oas" ruleset
  - https://unpkg.com/@stoplight/spectral-owasp-ruleset/dist/ruleset.mjs # OWASP API Security Top 10 - v2023

rules: 
  nlgov:use-problem-schema: off # application/problem+plain is not supported by the rule, disable for now
